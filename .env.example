# Virtual Office Environment Variables

# PostgreSQL Configuration
PGHOST=localhost
PGPORT=5432
PGDATABASE=virtual_office
PGUSER=postgres
PGPASSWORD=changeme

# Docker PostgreSQL (for docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# API Keys (Optional)
ANTHROPIC_API_KEY=
KIE_AI_API_KEY=

# Application
NODE_ENV=production
PORT=3210
